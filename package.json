{
  "name": "noken-api",
  "version": "1.0.0",
  "description": "api backend for noken letters game",
  "main": "dist/resolver.js",
  "scripts": {
    "prebuild": "tslint -c tslint.json -p tsconfig.json --fix",
    "build": "tsc",
    "prestart": "npm run build",
    "start": "node .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "github.com/kaiwah/letters-game-api"
  },
  "author": "kaiwah",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "@types/body-parser": "^1.17.1",
    "@types/express": "^4.17.2",
    "body-parser": "^1.19.0",
    "tslint": "^5.20.1",
    "typescript": "^3.7.2"
  }
}
